# V-JEPA-2-Planning
This project implements Meta’s V-JEPA 2 framework in PyTorch, building an Action-Conditioned Latent World Model. By processing directional actions with masked video, it learns to predict future environmental states entirely in embedding space, enabling pixel-free zero-shot planning.
Here is a professional, high-impact, and well-structured README.md for your GitHub repository. It perfectly balances a high-level overview with technical implementation details, incorporating the exact performance metrics from your successful training run.Action-Conditioned V-JEPA 2 World Model in PyTorchA clean, modular PyTorch implementation inspired by Meta FAIR's V-JEPA 2 (Joint-Embedding Predictive Architecture) framework. Moving away from traditional generative models that waste immense computational bandwidth decoding raw, high-dimensional pixels, this architecture implements an Action-Conditioned Latent World Model engineered for self-supervised temporal representation learning and zero-shot planning.🌌 Core Paradigm ShiftTraditional video prediction models rely on pixel-level autoencoders or diffusion processes, forcing the network to waste capacity modeling complex, irrelevant background noise.This repository shifts the learning objective to the Latent Space. By predicting transitions entirely within an abstract embedding manifold, the network ignores high-frequency pixel noise and focuses strictly on the semantic and causal structure of the scene:$$s_t, a_t \xrightarrow{\text{Predictor}} z_{t+1}$$🛠️ Architecture Mechanics1. Dual-Pathway Siamese NetworkThe architecture implements an asymmetric Siamese tracking framework to prevent representation collapse without requiring contrastive negative pairs:Context Encoder: A trainable convolutional backbone that maps the current visual observation $x_t$ to its latent representation $z_t$.Target Encoder: A structurally identical network that maps the target ground-truth frame $x_{t+1}$ to $z_{t+1}$. This path operates with strict stop-gradient isolation (requires_grad = False).Exponential Moving Average (EMA): The Target Encoder weights are updated dynamically via momentum tracking, ensuring stable targets throughout optimization:$$\theta_{\text{target}} \leftarrow \tau \theta_{\text{target}} + (1 - \tau) \theta_{\text{context}}$$2. Action-Conditioned PredictorInstead of guessing randomly masked blocks using coordinates alone, a Transformer-based dynamics network is conditioned on an execution variable (e.g., continuous directional velocity vectors). By projecting the continuous action $a_t$ and coupling it with $z_t$, the system models the explicit physical consequences of an action.📊 Optimization & Convergence PerformanceThe model maps low-entropy, deterministic environment physics with extreme sample efficiency. Because optimization runs completely within the latent manifold using a Mean Squared Error (MSE) criterion, the network rapidly masters the underlying dynamics.Pre-Training Loss: Feature-space MSE converges aggressively, dropping smoothly within 20 epochs to a stable plateau at ~0.000066.Anti-Collapse Verification: The stable curve layout confirms the momentum-based target encoder successfully eliminates representation degeneracy or vector collapse.
